const BASE_URL = 'http://localhost:8080/backend2-1.0-SNAPSHOT/api';

export const API_ENDPOINTS = {
    LOGIN: `${BASE_URL}/login`,
    REGISTER: `${BASE_URL}/register`,
    SEANCES: `${BASE_URL}/seances`,
    FILMS: `${BASE_URL}/films`,

    SALLESPROG: `${BASE_URL}/salles`,
    RESERVATIONS: `${BASE_URL}/reservations`,
    USER_BALANCE: `${BASE_URL}/users/balance`
};
